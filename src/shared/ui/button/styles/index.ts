import { Theme } from '@mui/material';
import { SxProps } from '@mui/system';

export const progress: SxProps<Theme> = {
  position: 'absolute',
  top: '50%',
  left: '50%',
  marginTop: -3,
  marginLeft: -3,
};
